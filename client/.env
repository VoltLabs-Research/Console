# Public origin of the console API, baked into the SPA at build time (Vite).
# Change this to your endpoint and rebuild the `console-client` image.
# For local dev, create .env.local (gitignored) with http://localhost:8081
VITE_CONSOLE_URL=https://console.voltcloud.dev
